import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import type { ContextStore } from './store.js';

export const registerSetContextTool = (
  _server: McpServer,
  _store: ContextStore,
): void => {
  // not implemented yet (red phase)
};
